spring:
  application:
    name: netflix-order
  profiles:
    active:
      dev
  cloud:
    config:
      uri: http://netflix-configuration-server.sa-east-1.elasticbeanstalk.com

server:
  port: 8092